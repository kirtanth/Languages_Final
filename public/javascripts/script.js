const subtopics = require('../../public/json/subtopics.json');

function myFunction() {
    var selectedLang = document.getElementById("selLang").value;
    document.getElementById("show").innerHTML = "You selected: " + selectedLang;
}

function abc() {
    var selected = document.getElementById("selLang").value;
    switch (selected) {
        case C:
            return name.C.topics
    }

}